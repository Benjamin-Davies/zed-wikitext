(heading1
  (heading_marker) @context
  (text) @name
  (heading_marker) @context
) @item
(heading2
  (heading_marker) @context
  (text) @name
  (heading_marker) @context
) @item
(heading3
  (heading_marker) @context
  (text) @name
  (heading_marker) @context
) @item
(heading4
  (heading_marker) @context
  (text) @name
  (heading_marker) @context
) @item
(heading5
  (heading_marker) @context
  (text) @name
  (heading_marker) @context
) @item
(heading6
  (heading_marker) @context
  (text) @name
  (heading_marker) @context
) @item
